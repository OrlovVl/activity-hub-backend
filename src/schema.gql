# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Category {
  description: String
  id: Int!
  name: String!
}

input CreatePostDto {
  content: String!
  location: String
  subcategoryId: Int!
  tags: [String!]
  title: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type HomePageData {
  categories: [Category!]!
  me: User
  trendingPosts: [Post!]!
}

type Mutation {
  addBookmark(postId: Int!): Boolean!
  createPost(data: CreatePostDto!): Post!
  deletePost(id: Int!): Boolean!
  updateMe(data: UpdateUserDto!): User!
  updatePost(data: UpdatePostDto!, id: Int!): Post!
}

type Post {
  author: User!
  content: String!
  createdAt: DateTime!
  id: Int!
  likesCount: Int!
  title: String!
}

type Query {
  getHomePage: HomePageData!
  me: User!
  post(id: Int!): Post!
  posts(limit: Int! = 10, subcategoryId: Int): [Post!]!
  users(limit: Int! = 10, search: String): [User!]!
}

enum Role {
  ADMIN
  MODERATOR
  USER
}

input UpdatePostDto {
  content: String
  location: String
  subcategoryId: Int
  tags: [String!]
  title: String
}

input UpdateUserDto {
  bio: String
  email: String
  username: String
}

type User {
  createdAt: DateTime!
  email: String!
  id: Int!
  role: Role!
  username: String!
}